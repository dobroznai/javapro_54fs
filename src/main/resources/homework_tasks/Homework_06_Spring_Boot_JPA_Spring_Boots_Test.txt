Цель работы
Закрепить умение писать интеграционные тесты REST-контроллеров на Spring Boot

Научиться тестировать разные сценарии поведения API: успешные запросы, пустые результаты, ошибки валидации, фильтры и границы диапазонов.

Описание проекта
Контроллер RestApiCarController предоставляет REST-эндпоинты для работы с автомобилями:

/api/cars — получить все автомобили
/api/cars/color/{color} — получить автомобили по цвету (регистронезависимо)
/api/cars/price/between/{min}/{max} — получить автомобили по диапазону цен
/api/cars/price/under/{max} — получить автомобили дешевле или равные указанной цене
/api/cars/price/over/{min} — получить автомобили дороже или равные указанной цене
Для тестирования контроллера необходимо создать интеграционные тесты, проверяющие все GET-методы и все возможные сценарии ответов.

Что нужно сделать
Настроить тестовую среду:
профиль test
база данных H2 (или аналогичная in-memory)
тестовые SQL-скрипты:

sql/clear.sql — очистка таблицы cars
sql/seed_cars.sql — загрузка данных для позитивных сценариев
Написать интеграционные тесты (MockMvc):
один общий тест-класс RestApiCarControllerIT
использовать аннотации:

@SpringBootTest
@AutoConfigureMockMvc
@ActiveProfiles("test")
@Sql(...) для подготовки данных
Для каждого GET-эндпоинта проверить:
корректный HTTP-статус
корректный тип контента (application/json)
корректную структуру и содержимое JSON-ответа
Тестовые сценарии
1️⃣
GET /api/cars
Описание: возвращает список всех автомобилей из базы данных.

Сценарии:

✅ Данные есть:

Ожидаем статус 200 OK, JSON-массив содержит все записи из тестовой таблицы.
⚙️ Данных нет:

Ожидаем статус 200 OK, возвращается пустой массив [].
2️⃣
GET /api/cars/color/{color}
Описание: фильтрует автомобили по цвету, регистр букв не имеет значения.

Сценарии:

✅ Цвет найден (регистр не важен):

При запросе /api/cars/color/red или /api/cars/color/ReD возвращаются все автомобили, у которых цвет red или RED.

Статус 200 OK, все элементы содержат поле "color" равное указанному.
⚙️ Цвет не найден:

При запросе /api/cars/color/purple возвращается пустой массив.

Статус 404 NOT_FOUND.
3️⃣
GET /api/cars/price/between/{min}/{max}
Описание: возвращает автомобили, у которых цена находится в заданном диапазоне (включительно).

Сценарии:

✅ Автомобили найдены:

/api/cars/price/between/10000/30000 → возвращает все авто, где price между 10 000 и 30 000 (включительно).

Статус 200 OK.
⚙️ Нет подходящих по цене:

/api/cars/price/between/100/500 → массив пустой, статус 404 NOT_FOUND.
❌ Некорректный диапазон (max < min):

/api/cars/price/between/30000/10000 → статус 400 BAD_REQUEST, массив пустой.
🧭 Граничные значения:

Проверить, что автомобили с ценой ровно min и max включаются в результат.
4️⃣
GET /api/cars/price/under/{max}
Описание: возвращает автомобили, у которых цена меньше или равна max.

Сценарии:

✅ Есть подходящие автомобили:

/api/cars/price/under/20000 → статус 200 OK, все значения price ≤ 20000.
⚙️ Нет таких:

/api/cars/price/under/1000 → статус 404 NOT_FOUND, пустой массив.
5️⃣
GET /api/cars/price/over/{min}
Описание: возвращает автомобили, у которых цена больше или равна min.

Сценарии:

✅ Есть подходящие автомобили:

/api/cars/price/over/25000 → статус 200 OK, все price ≥ 25000.
⚙️ Нет таких:

/api/cars/price/over/1000000 → статус 404 NOT_FOUND, пустой массив.
Проверки, обязательные в каждом тесте
HTTP-статус ответа (200 / 404 / 400).
Content-Type: application/json.
Проверка размера массива jsonPath("$", ...).
Проверка условий фильтрации:
color совпадает с переданным (без учёта регистра);
price находится в заданных границах;
пустой массив при отсутствии записей.
Для 404-ответов убедиться, что тело действительно [].
Дополнительные указания
Каждый тест должен быть изолирован (через @Sql выполняются clear.sql и при необходимости seed_cars.sql).
Имена тестов должны чётко отражать суть:

shouldReturnCarsWhenDataExists(),

shouldReturnEmptyListWhenNoCars(),

shouldReturn404WhenColorNotFound() и т.д.